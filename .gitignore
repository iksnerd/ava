# Build artifacts
/bin/
/dist/
/local-whisper

# Python / mlx-engine
mlx-engine/.venv/
mlx-engine/__pycache__/
mlx-engine/**/__pycache__/

# Python (generic — catches any subproject added later, not just the three below)
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# Swift
AvaMenuBar/.build/

# Python (voxtral/)
voxtral/.venv/
voxtral/__pycache__/

# Python (scripts/voice_hooks/)
scripts/voice_hooks/.venv/
scripts/voice_hooks/__pycache__/
scripts/voice_hooks/**/__pycache__/
scripts/voice_hooks/.pytest_cache/
scripts/voice_hooks/.ruff_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Secrets — nothing here is read by this project today; this is a guard, not a fix
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# Agent-session local state (per-machine, not per-project)
.claude/settings.local.json

# Private working notes — kept out of the published repo on purpose
/TODO.md
/SETUP.md

# Transcripts: output, not source. mlx-engine/transcript.txt slipped into a
# commit this way once, from a manual `local-whisper transcribe` run.
transcript.txt
transcript-*.txt
